# === Secrets / machine-local agent + MCP config (NEVER commit — contain API keys) ===
.mcp.json
.codex/
.mswai/
.agents/
.claude/

# === MSW engine / Nexon-provided (third-party IP, regenerated per workspace) ===
Environment/
Global/

# === Generated artifacts (regenerated by Maker Refresh) ===
*.codeblock

# === OS / editor junk ===
.DS_Store
*.swp
*.swo
Thumbs.db
